# Copy to .env and adjust. Every variable here is validated at boot by src/config/env.schema.ts.
NODE_ENV=development
PORT=3000
# Matches docker-compose.yml. Local-only credentials — never reuse in a deployed environment.
DATABASE_URL=postgresql://split:localdev@localhost:5432/split
